# CineCrave
Movie Recommendation System using vector embedding

use tmdb 5000 movie data set as base line data set . in future as the model with base line data set has completed and the pipe set will jump onto the bigger dataset of movieLens wil 5Millon data points.
Just Clean the dataset for the vector embadding. tommorow will do the vectorization.
